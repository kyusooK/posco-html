package com.posco.carmanagement.s20a01.domain.car.mybatis;

import java.util.List;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface CarMapper {
    // 예시
    // List<CarMybatisEntity> selectCarList();
    // CarMybatisEntity selectCar(Long id);
}
